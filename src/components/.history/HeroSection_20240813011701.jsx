import ImageSlider from '/'
import HeroText from './HeroText'
const HeroSection = () => {
  return (
    <section>
      <ImageSlider />
      <HeroText />

    </section>
  )
}

export default HeroSection