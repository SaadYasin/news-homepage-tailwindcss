import ImageSlider from './ImageSlider'
import HeroText from './HeroText'
const HeroSection = () => {
  return (
    <section>
      <ImageSlider />
      <HeroText />

    </section>
  )
}

export default HeroSection